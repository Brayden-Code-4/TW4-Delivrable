<?xml version="1.0" encoding="utf-8"?><?xml-stylesheet type="text/xsl" href="rss.xsl"?>
<rss version="2.0" xmlns:dc="http://purl.org/dc/elements/1.1/" xmlns:content="http://purl.org/rss/1.0/modules/content/">
    <channel>
        <title>TW4 Blog</title>
        <link>https://brayden-code-4.github.io/TW4-Delivrable/blog</link>
        <description>TW4 Blog</description>
        <lastBuildDate>Tue, 22 Sep 2026 00:00:00 GMT</lastBuildDate>
        <docs>https://validator.w3.org/feed/docs/rss2.html</docs>
        <generator>https://github.com/jpmonette/feed</generator>
        <language>en</language>
        <item>
            <title><![CDATA[You don't need Postgres to track a writing backlog]]></title>
            <link>https://brayden-code-4.github.io/TW4-Delivrable/blog/no-database-task-api</link>
            <guid>https://brayden-code-4.github.io/TW4-Delivrable/blog/no-database-task-api</guid>
            <pubDate>Tue, 22 Sep 2026 00:00:00 GMT</pubDate>
            <description><![CDATA[This article explains why TW4 is a one-file HTTP API with a JSON store, how a request actually moves through the process, and how to run the same thing on Node or in Docker. If you are about to add Redis "just in case", read this first.]]></description>
            <content:encoded><![CDATA[<p>This article explains why TW4 is a one-file HTTP API with a JSON store, how a request actually moves through the process, and how to run the same thing on Node or in Docker. If you are about to add Redis "just in case", read this first.</p>
<!-- -->
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="the-problem">The problem<a href="https://brayden-code-4.github.io/TW4-Delivrable/blog/no-database-task-api#the-problem" class="hash-link" aria-label="Direct link to The problem" title="Direct link to The problem" translate="no">​</a></h2>
<p>I wanted a small backlog for documentation work: a title, a status (<code>todo</code> / <code>doing</code> / <code>done</code>), and an id I can paste into a terminal. The usual stack for that is Express + Postgres + an ORM + a <code>.env</code> with twelve variables you copy from a wiki.</p>
<p>That stack is fine when you have users and backups. It is not fine when you just need <code>curl</code> to create a row in under five minutes. The extra moving parts also make the docs worse. You start explaining migrations before the reader has a <code>200</code> on <code>/health</code>.</p>
<p>TW4 is the other extreme on purpose: Node's <code>http</code> module, no npm dependencies at runtime, one JSON file on disk. The process stays simple enough that the README, the install guide and the API pages can tell the same story.</p>
<p>Repo: <a href="https://github.com/Brayden-Code-4/TW4-Delivrable" target="_blank" rel="noopener noreferrer" class="">github.com/Brayden-Code-4/TW4-Delivrable</a>.</p>
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="what-no-database-actually-means">What "no database" actually means<a href="https://brayden-code-4.github.io/TW4-Delivrable/blog/no-database-task-api#what-no-database-actually-means" class="hash-link" aria-label="Direct link to What &quot;no database&quot; actually means" title="Direct link to What &quot;no database&quot; actually means" translate="no">​</a></h2>
<p>On start, <code>src/server.js</code> reads <code>.env</code> (only keys that are not already in the environment), then ensures <code>data/tasks.json</code> exists. If the file is missing, it writes <code>[]</code>.</p>
<p>Every list or write loads the file, mutates an array in memory, and writes it back. There is no connection pool and no migration folder. There is also no lock. Two processes on the same file will race. For a local tracker that is an acceptable trade. For a team of twenty, it is not. Say that in the docs before someone deploys it on a shared VM and wonders why tasks vanish.</p>
<p>A task looks like this:</p>
<div class="language-json codeBlockContainer_Ckt0 theme-code-block" style="--prism-color:#393A34;--prism-background-color:#f6f8fa"><div class="codeBlockContent_QJqH"><pre tabindex="0" class="prism-code language-json codeBlock_bY9V thin-scrollbar" style="color:#393A34;background-color:#f6f8fa"><code class="codeBlockLines_e6Vv"><div class="token-line" style="color:#393A34"><span class="token punctuation" style="color:#393A34">{</span><span class="token plain"></span><br></div><div class="token-line" style="color:#393A34"><span class="token plain">  </span><span class="token property" style="color:#36acaa">"id"</span><span class="token operator" style="color:#393A34">:</span><span class="token plain"> </span><span class="token string" style="color:#e3116c">"78cb84f4-f939-4729-8c7b-2e3fddedd05b"</span><span class="token punctuation" style="color:#393A34">,</span><span class="token plain"></span><br></div><div class="token-line" style="color:#393A34"><span class="token plain">  </span><span class="token property" style="color:#36acaa">"title"</span><span class="token operator" style="color:#393A34">:</span><span class="token plain"> </span><span class="token string" style="color:#e3116c">"Write the user guide"</span><span class="token punctuation" style="color:#393A34">,</span><span class="token plain"></span><br></div><div class="token-line" style="color:#393A34"><span class="token plain">  </span><span class="token property" style="color:#36acaa">"status"</span><span class="token operator" style="color:#393A34">:</span><span class="token plain"> </span><span class="token string" style="color:#e3116c">"todo"</span><span class="token punctuation" style="color:#393A34">,</span><span class="token plain"></span><br></div><div class="token-line" style="color:#393A34"><span class="token plain">  </span><span class="token property" style="color:#36acaa">"createdAt"</span><span class="token operator" style="color:#393A34">:</span><span class="token plain"> </span><span class="token string" style="color:#e3116c">"2026-09-20T17:50:54.691Z"</span><span class="token punctuation" style="color:#393A34">,</span><span class="token plain"></span><br></div><div class="token-line" style="color:#393A34"><span class="token plain">  </span><span class="token property" style="color:#36acaa">"updatedAt"</span><span class="token operator" style="color:#393A34">:</span><span class="token plain"> </span><span class="token string" style="color:#e3116c">"2026-09-20T17:50:54.691Z"</span><span class="token plain"></span><br></div><div class="token-line" style="color:#393A34"><span class="token plain"></span><span class="token punctuation" style="color:#393A34">}</span><br></div></code></pre></div></div>
<p>The <code>id</code> is <code>crypto.randomUUID()</code>. The timestamps are <code>toISOString()</code>. Status is one of three strings. If you send <code>wip</code>, you get <code>400</code>, not a silent coerce.</p>
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="how-a-write-request-walks-the-code">How a write request walks the code<a href="https://brayden-code-4.github.io/TW4-Delivrable/blog/no-database-task-api#how-a-write-request-walks-the-code" class="hash-link" aria-label="Direct link to How a write request walks the code" title="Direct link to How a write request walks the code" translate="no">​</a></h2>
<!-- -->
<p>GET <code>/health</code> and GET <code>/api/v1/tasks</code> skip the key. That is a product choice: you can health-check and list without leaking the secret into every CI ping. Writes cannot.</p>
<p>Two details that ate time when I documented the API:</p>
<ol>
<li class="">
<p>Task ids in the URL must match a UUID regex (<code>[0-9a-fA-F-]{36}</code>). <code>GET /api/v1/tasks/1</code> is not "task not found". It is "no route". The 404 message says <code>No route for GET /api/v1/tasks/1</code>. If you document only "unknown id", readers will think the row is missing.</p>
</li>
<li class="">
<p>Node lowercases headers. You send <code>X-API-Key</code>. The code reads <code>req.headers["x-api-key"]</code>. <code>Authorization: Bearer</code> does nothing.</p>
</li>
</ol>
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="run-it-natively">Run it natively<a href="https://brayden-code-4.github.io/TW4-Delivrable/blog/no-database-task-api#run-it-natively" class="hash-link" aria-label="Direct link to Run it natively" title="Direct link to Run it natively" translate="no">​</a></h2>
<p>Node 20 or newer. Copy <code>.env.example</code> to <code>.env</code> and replace the example key with yours if you share the machine.</p>
<div class="language-bash codeBlockContainer_Ckt0 theme-code-block" style="--prism-color:#393A34;--prism-background-color:#f6f8fa"><div class="codeBlockContent_QJqH"><pre tabindex="0" class="prism-code language-bash codeBlock_bY9V thin-scrollbar" style="color:#393A34;background-color:#f6f8fa"><code class="codeBlockLines_e6Vv"><div class="token-line" style="color:#393A34"><span class="token plain">git clone https://github.com/Brayden-Code-4/TW4-Delivrable.git</span><br></div><div class="token-line" style="color:#393A34"><span class="token plain">cd TW4</span><br></div><div class="token-line" style="color:#393A34"><span class="token plain">cp .env.example .env</span><br></div><div class="token-line" style="color:#393A34"><span class="token plain">npm install</span><br></div><div class="token-line" style="color:#393A34"><span class="token plain">npm start</span><br></div></code></pre></div></div>
<p><code>npm install</code> does not pull a tree of packages. The <code>package.json</code> has no runtime dependencies. Then:</p>
<div class="language-bash codeBlockContainer_Ckt0 theme-code-block" style="--prism-color:#393A34;--prism-background-color:#f6f8fa"><div class="codeBlockContent_QJqH"><pre tabindex="0" class="prism-code language-bash codeBlock_bY9V thin-scrollbar" style="color:#393A34;background-color:#f6f8fa"><code class="codeBlockLines_e6Vv"><div class="token-line" style="color:#393A34"><span class="token plain">curl http://127.0.0.1:3000/health</span><br></div></code></pre></div></div>
<p>You want <code>"status":"ok"</code>. Keep that terminal open. In a second one:</p>
<div class="language-bash codeBlockContainer_Ckt0 theme-code-block" style="--prism-color:#393A34;--prism-background-color:#f6f8fa"><div class="codeBlockContent_QJqH"><pre tabindex="0" class="prism-code language-bash codeBlock_bY9V thin-scrollbar" style="color:#393A34;background-color:#f6f8fa"><code class="codeBlockLines_e6Vv"><div class="token-line" style="color:#393A34"><span class="token plain">curl -s -X POST http://127.0.0.1:3000/api/v1/tasks \</span><br></div><div class="token-line" style="color:#393A34"><span class="token plain">  -H "Content-Type: application/json" \</span><br></div><div class="token-line" style="color:#393A34"><span class="token plain">  -H "X-API-Key: change-me-now" \</span><br></div><div class="token-line" style="color:#393A34"><span class="token plain">  --data-binary '{"title":"Ship the blog post","status":"todo"}'</span><br></div></code></pre></div></div>
<p>PowerShell users: do not paste the JSON inside <code>{ }</code> as a script block. Use <code>ConvertTo-Json</code> and <code>Invoke-RestMethod</code>, or the request body will not be JSON and you get <code>400 invalid_json</code>. That bug is the host, not TW4.</p>
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="same-api-in-docker">Same API in Docker<a href="https://brayden-code-4.github.io/TW4-Delivrable/blog/no-database-task-api#same-api-in-docker" class="hash-link" aria-label="Direct link to Same API in Docker" title="Direct link to Same API in Docker" translate="no">​</a></h2>
<p>If you do not want Node on the host:</p>
<div class="language-bash codeBlockContainer_Ckt0 theme-code-block" style="--prism-color:#393A34;--prism-background-color:#f6f8fa"><div class="codeBlockContent_QJqH"><pre tabindex="0" class="prism-code language-bash codeBlock_bY9V thin-scrollbar" style="color:#393A34;background-color:#f6f8fa"><code class="codeBlockLines_e6Vv"><div class="token-line" style="color:#393A34"><span class="token plain">cp .env.example .env</span><br></div><div class="token-line" style="color:#393A34"><span class="token plain">docker compose up --build</span><br></div></code></pre></div></div>
<p>Compose publishes port 3000, mounts a named volume on <code>/app/data</code>, and hits <code>/health</code> as a healthcheck. <code>HOST</code> inside the container must remain <code>0.0.0.0</code> or the port mapping never reaches the process.</p>
<p>The client commands do not change. <code>http://127.0.0.1:3000/health</code> is still the check. That is the point of a small HTTP API: one contract, two ways to start the process.</p>
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="what-i-would-document-again-and-what-i-would-not">What I would document again (and what I would not)<a href="https://brayden-code-4.github.io/TW4-Delivrable/blog/no-database-task-api#what-i-would-document-again-and-what-i-would-not" class="hash-link" aria-label="Direct link to What I would document again (and what I would not)" title="Direct link to What I would document again (and what I would not)" translate="no">​</a></h2>
<p>Document the auth header, the three statuses, the UUID rule, and the empty-<code>API_KEY</code> → <code>500</code> case. Those are the four things that fail on a clean clone.</p>
<p>Do not document Express middleware, ORMs, or "how to scale to Kubernetes" for this repo. You would be writing fiction. If the store becomes SQLite later, add a page then. Until that commit exists, a JSON file is the architecture.</p>
<p>A tiny client is enough as an SDK. Node 20 <code>fetch</code> and Python <code>urllib</code> both work. Keep the key in <code>TW4_API_KEY</code> in the caller so it does not clash with the server's own <code>API_KEY</code> if you run both in the same shell.</p>
<p>If something still looks wrong after <code>/health</code> is ok, check the key first, then the JSON body, then whether the id is a real UUID. In that order. Most "the API is down" reports are one of those three.</p>
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="why-this-is-easier-to-explain-than-a-real-backend">Why this is easier to explain than a "real" backend<a href="https://brayden-code-4.github.io/TW4-Delivrable/blog/no-database-task-api#why-this-is-easier-to-explain-than-a-real-backend" class="hash-link" aria-label="Direct link to Why this is easier to explain than a &quot;real&quot; backend" title="Direct link to Why this is easier to explain than a &quot;real&quot; backend" translate="no">​</a></h2>
<p>When I wrote the install pages, the native path and the Docker path could share the same <code>curl</code> examples. That only works if the public contract is HTTP. The moment you document "run <code>psql</code> then <code>npm run migrate</code>", you split the audience in two and you maintain two truths.</p>
<p>The JSON file also forces you to be honest in the architecture page. You cannot hide behind "the database handles consistency". You have to say: one writer, one file, reload on every request. Readers who need more can fork. Readers who need less can finish in fifteen minutes.</p>
<p>I would still add a <code>GET /api/v1/tasks/:id</code> example with a fake short id (<code>1</code>) in the troubleshooting table. It is the fastest way to show the UUID regex. One failed command in the docs is worth a paragraph of theory.</p>
<p>The same idea applies to auth. Show the 401 body. Show the 500 body when <code>API_KEY</code> is missing. Do not only show the happy <code>201</code>. People copy the happy path, forget the header, and decide the project is broken.</p>
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="links">Links<a href="https://brayden-code-4.github.io/TW4-Delivrable/blog/no-database-task-api#links" class="hash-link" aria-label="Direct link to Links" title="Direct link to Links" translate="no">​</a></h2>
<ul>
<li class=""><a class="" href="https://brayden-code-4.github.io/TW4-Delivrable/docs/quickstart">Quickstart</a></li>
<li class=""><a class="" href="https://brayden-code-4.github.io/TW4-Delivrable/docs/installation">Installation</a></li>
<li class=""><a class="" href="https://brayden-code-4.github.io/TW4-Delivrable/docs/api/overview">API overview</a></li>
<li class=""><a class="" href="https://brayden-code-4.github.io/TW4-Delivrable/docs/developer/code-reference">Code reference</a></li>
</ul>]]></content:encoded>
            <category>TW4</category>
            <category>Node.js</category>
            <category>Docker</category>
        </item>
    </channel>
</rss>